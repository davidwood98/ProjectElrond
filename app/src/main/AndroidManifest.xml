<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Calendar integration (Phase 5): DeviceCalendarProvider reads/writes the
         device calendar. Requested at runtime; only used after explicit user action. -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- Stylus is the primary input but the app must still install without one -->
    <uses-feature
        android:name="android.hardware.touchscreen.stylus"
        android:required="false" />

    <!-- allowBackup=false: note content stays on-device, never in cloud backups. -->
    <application
        android:name=".ElrondApplication"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectElrond">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outlook OAuth (FA-11): MSAL captures the browser redirect after interactive sign-in
             via this activity. The scheme/host/path must match the redirect URI registered for the
             Azure app ("Android" platform). The path is the base64 package-signature hash, injected
             from local.properties (outlook.signatureHash) via the msalRedirectPath placeholder; a
             harmless placeholder keeps the manifest valid when Outlook isn't configured. -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="msauth"
                    android:host="${applicationId}"
                    android:path="/${msalRedirectPath}" />
            </intent-filter>
        </activity>

        <!-- @HiltWorker needs the Hilt WorkerFactory (provided via Configuration.Provider on
             ElrondApplication), so remove WorkManager's default on-demand initializer. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
